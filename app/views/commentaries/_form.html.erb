<%= form_for(@commentary) do |f| %>
  <% if @commentary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commentary.errors.count, "error") %> prohibited this commentary from being saved:</h2>

      <ul>
      <% @commentary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <% f.label :user_id %><br>
    <%= f.hidden_field :user_id, :value => @cookie %>
  </div>
  <div class="field">
    <% f.label :article_id %><br>
    <%= f.hidden_field :article_id, :value => @article %>
  </div>
  <div class="field">
    <%= f.label :Commentaire %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
